{
  "name": "modal-stack-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "pack:all": "pnpm -r run pack:test"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
